{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm build",
  "installCommand": "cd .. && pnpm install --frozen-lockfile && cd app",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ./ ../package.json ../pnpm-lock.yaml",
  "regions": ["iad1"]
}
